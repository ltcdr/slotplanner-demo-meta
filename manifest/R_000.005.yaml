release: R_000.005

services:
  slotplanner-demo: R_000.005
  slotplanner-demo-functions: R_000.005

deployment-order:
  - slotplanner-demo-functions
  - slotplanner-demo

rollback:
  coordinated: true

audit:
  created_by: ltcdr
  date: 2026-08-15
  notes: |
    Stable end-to-end demo release including protected admin endpoints via Managed Identity, 
    validated bearer token flow for Azure Functions, 
    stable dependency sets, updated documentation, 
    and reliable weekly activity generation and daily cleanup jobs. 
    CI/CD via OIDC is fully operational. 
    Tests and a Functions frontend are planned for future releases.
